<!-- mapper/chats.xml -->

<mapper namespace="chats">
    <insert id="insert">
        INSERT INTO fucai.chats (
        id,
        creator_id,
        name
        ) VALUES (
        #{id},
        #{creatorId},
        #{name}
        )
    </insert>

    <select id="selectById" resultType="object">
        SELECT * FROM fucai.chats WHERE id = #{id}
    </select>

    <update id="updateById">
        UPDATE fucai.chats SET
        name = #{name}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM fucai.chats WHERE id = #{id}
    </delete>

    <select id="selectParticipantsByChatId" resultType="object">
        SELECT
        characters.id,
        characters.slug,
        characters.name,
        characters.avatar_id
        FROM
        fucai.chat_participants,
        fucai.characters
        WHERE
        chat_participants.chat_id = #{chatId} AND
        chat_participants.character_id = characters.id
    </select>

    <insert id="addParticipant">
        INSERT INTO fucai.chat_participants (
        chat_id,
        character_id
        ) VALUES (
        #{chatId},
        #{characterId}
        )
    </insert>

    <delete id="removeParticipant">
        DELETE FROM fucai.chat_participants WHERE chat_id = #{chatId} AND character_id = #{characterId}
    </delete>
</mapper>
